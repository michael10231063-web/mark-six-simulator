const CACHE="mark-six-sim-v2";const BASE=new URL(self.registration.scope).pathname.replace(/\/$/,"");const CORE=[`${BASE}/`,`${BASE}/manifest.webmanifest`,`${BASE}/icon-192.png`,`${BASE}/apple-touch-icon.png`,`${BASE}/latest-result.json`];
self.addEventListener("install",e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(CORE)));self.skipWaiting()});
self.addEventListener("activate",e=>{e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))));self.clients.claim()});
self.addEventListener("fetch",e=>{if(e.request.method!=="GET")return;const u=new URL(e.request.url);if(u.pathname.endsWith("/api/latest-result")||u.pathname.endsWith("/latest-result.json")||e.request.mode==="navigate"){e.respondWith(fetch(e.request).then(r=>{const clone=r.clone();caches.open(CACHE).then(c=>c.put(e.request,clone));return r}).catch(()=>caches.match(e.request).then(c=>c||caches.match(`${BASE}/`))));return}e.respondWith(caches.match(e.request).then(c=>c||fetch(e.request)))});
